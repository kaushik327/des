[package]
name = "des"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.1"
clap = { version = "4.4", features = ["derive"] }

[lints.rust]
unsafe_code = "forbid"
missing_debug_implementations = "warn"

[lints.clippy]
all = { level = "warn", priority = -1 }
